meta {
  name: Entrar como seller
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
}

body:json {
  {
    "email": "{{emailSellerInsumos}}",
    "password": "contrasena-de-prueba-123"
  }
}

docs {
  Cambia de sesión. Los dos requests siguientes verifican las dos mitades: que
  el `seller` no puede tocar los insumos, y que el `pos` sí — porque es quien
  produce y sin tapas no envasa.
}

tests {
  test("entra", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
