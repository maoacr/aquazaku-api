meta {
  name: Volver como admin otra vez
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
}

body:json {
  {
    "email": "{{adminEmail}}",
    "password": "{{adminPassword}}"
  }
}

docs {
  Deja la colección como la encontró. Sin esto, 09-Ventas arrancaría con la
  sesión del `pos` y fallaría con 403 que no tienen nada que ver con ventas.
}

tests {
  test("vuelve a ser admin", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
