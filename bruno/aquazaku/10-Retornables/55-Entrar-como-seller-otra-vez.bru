meta {
  name: Entrar como seller, otra vez
  type: http
  seq: 55
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
}

body:json {
  {
    "email": "seller-retornables@aquazaku.com",
    "password": "contrasena-de-prueba-123"
  }
}

docs {
  El pedido que sigue es el más importante de esta carpeta, y necesita a alguien
  que pueda vender pero no prestar bases. El `seller` es exactamente eso.
}

tests {
  test("entra", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
