meta {
  name: Y ya no figura en la lista
  type: http
  seq: 37
}

get {
  url: {{baseUrl}}/bases
}

docs {
  Queda la 0914. La 0913 sigue en la base de datos con su historial y su
  recargo, pero no en el parque — que es exactamente la diferencia entre
  `activa: false` y un `DELETE`.
}

tests {
  test("solo queda la que sigue activa", function() {
    expect(res.getBody()).to.have.lengthOf(1);
    expect(res.getBody()[0].idSticker).to.equal("0914");
  });
}
